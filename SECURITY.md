# 安全上线建议

公开上线前建议完成：

- 开启 HTTPS。
- 在反向代理或平台层限制请求体大小。
- 启用验证码、登录或额度限制，防止批量滥用。
- 不保存上传文件、处理结果和原始文件名。
- 不把临时目录挂载到持久磁盘。
- 配置磁盘和内存监控。
- 定期更新 Python 依赖。

当前应用内置：

- PDF 扩展名和 `%PDF` 文件头检查。
- 单次上传总大小限制。
- 单次文件数量限制。
- 简单 IP 每分钟请求频率限制。
- 临时目录自动清理。
